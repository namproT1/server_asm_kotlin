{
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "start": "json-server --host  10.24.32.82 --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
